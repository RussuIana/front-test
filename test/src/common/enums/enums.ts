
export enum MoviePopularity {
    Popular = 0,
    TopRated = 1,
    Upcoming = 2,
    NowPlaying = 3
}

export enum MoviePriority {
    Low = 0,
    Middle = 1,
    Hi = 2,
    Urgently = 3,
    Later = 4,
}

export enum ResultCode {
    Success = 0,
    Error = 1,

}
