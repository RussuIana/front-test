import type {SxProps} from "@mui/material";

export const posterSx : SxProps  = {
    width: { xs: "100%", sm: 300 },
    height: { xs: "auto", sm: 450 },
    borderRadius: "5%",
        objectFit: "cover",
        flexShrink: 0,
}